<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"
     viewBox="0 0 900 450" width="900" height="450" role="img"
     aria-label="Nithish-O7 — Command Center Banner">

  <defs>
    <!-- Deep space background gradient -->
    <radialGradient id="bgGlow" cx="50%" cy="40%" r="65%">
      <stop offset="0%"   stop-color="#0d1b2a" stop-opacity="1"/>
      <stop offset="70%"  stop-color="#0a1117" stop-opacity="1"/>
      <stop offset="100%" stop-color="#060d13" stop-opacity="1"/>
    </radialGradient>

    <!-- Left panel gradient -->
    <linearGradient id="panelGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%"   stop-color="#0f1e2e" stop-opacity="0.85"/>
      <stop offset="100%" stop-color="#0a1520" stop-opacity="0.65"/>
    </linearGradient>

    <!-- Right panel gradient -->
    <linearGradient id="panelGradR" x1="100%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%"   stop-color="#15102a" stop-opacity="0.85"/>
      <stop offset="100%" stop-color="#0d0a1e" stop-opacity="0.65"/>
    </linearGradient>

    <!-- Cyan glow filter for ASCII name — very subtle -->
    <filter id="nameGlow" x="-5%" y="-20%" width="110%" height="140%">
      <feGaussianBlur stdDeviation="0.4" result="blur1"/>
      <feMerge>
        <feMergeNode in="blur1"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- Node glow -->
    <filter id="nodeGlow" x="-60%" y="-60%" width="220%" height="220%">
      <feGaussianBlur stdDeviation="5" result="blur"/>
      <feMerge>
        <feMergeNode in="blur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- Purple glow -->
    <filter id="purpleGlow" x="-60%" y="-60%" width="220%" height="220%">
      <feGaussianBlur stdDeviation="4" result="blur"/>
      <feMerge>
        <feMergeNode in="blur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- Subtle glow for badges -->
    <filter id="badgeGlow" x="-20%" y="-60%" width="140%" height="220%">
      <feGaussianBlur stdDeviation="2" result="blur"/>
      <feMerge>
        <feMergeNode in="blur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- Clip path -->
    <clipPath id="svgClip">
      <rect width="900" height="450" rx="14"/>
    </clipPath>

    <!-- Grid pattern -->
    <pattern id="grid" x="0" y="0" width="45" height="45" patternUnits="userSpaceOnUse">
      <path d="M 45 0 L 0 0 0 45" fill="none" stroke="#00d4ff" stroke-width="0.18" opacity="0.1"/>
    </pattern>

    <!-- Hex grid pattern -->
    <pattern id="hexGrid" x="0" y="0" width="30" height="52" patternUnits="userSpaceOnUse">
      <path d="M15,0 L30,8.66 L30,26 L15,34.64 L0,26 L0,8.66 Z" fill="none" stroke="#8a2be2" stroke-width="0.12" opacity="0.07"/>
    </pattern>

    <!-- Data flow gradients -->
    <linearGradient id="lineGrad1" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#8a2be2" stop-opacity="0.85"/>
      <stop offset="100%" stop-color="#00f0ff" stop-opacity="0.95"/>
    </linearGradient>
    <linearGradient id="lineGrad2" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%"   stop-color="#00f0ff" stop-opacity="0.95"/>
      <stop offset="100%" stop-color="#8a2be2" stop-opacity="0.85"/>
    </linearGradient>

    <!-- Bottom bar gradient -->
    <linearGradient id="bottomGrad" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%"   stop-color="#0f1e2e" stop-opacity="0.9"/>
      <stop offset="50%"  stop-color="#15102a" stop-opacity="0.95"/>
      <stop offset="100%" stop-color="#0f1e2e" stop-opacity="0.9"/>
    </linearGradient>
  </defs>

  <style>
    .font-inter { font-family: 'Inter', 'Segoe UI', system-ui, sans-serif; }
    .font-mono  { font-family: 'Share Tech Mono', 'Courier New', Courier, Monaco, monospace; }

    /* ── Radar sweep ── */
    @keyframes radarSweep {
      0%   { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    .radar-sweep {
      transform-origin: 718px 205px;
      animation: radarSweep 3.5s linear infinite;
    }

    /* ── Central node pulse ── */
    @keyframes nodePulse {
      0%   { transform: scale(1);    opacity: 1; }
      50%  { transform: scale(1.2);  opacity: 0.6; }
      100% { transform: scale(1);    opacity: 1; }
    }
    .node-pulse {
      transform-origin: 718px 205px;
      animation: nodePulse 2.4s ease-in-out infinite;
    }

    /* ── Outer ring pulse ── */
    @keyframes ringPulse {
      0%   { r: 30; opacity: 0.7; }
      50%  { r: 52; opacity: 0.0; }
      100% { r: 30; opacity: 0.7; }
    }
    .ring-pulse { animation: ringPulse 2.4s ease-out infinite; }

    @keyframes ringPulse2 {
      0%   { r: 30; opacity: 0.4; }
      50%  { r: 64; opacity: 0.0; }
      100% { r: 30; opacity: 0.4; }
    }
    .ring-pulse-2 { animation: ringPulse2 2.4s ease-out 0.8s infinite; }

    /* ── Satellite pulse ── */
    @keyframes satPulse {
      0%,100% { opacity: 1; }
      50%     { opacity: 0.35; }
    }
    .sat-a { animation: satPulse 3.2s ease-in-out 0.0s infinite; }
    .sat-b { animation: satPulse 3.2s ease-in-out 0.8s infinite; }
    .sat-c { animation: satPulse 3.2s ease-in-out 1.6s infinite; }
    .sat-d { animation: satPulse 3.2s ease-in-out 2.4s infinite; }

    /* ── Data flow ── */
    @keyframes flowDash {
      0%   { stroke-dashoffset: 140; }
      100% { stroke-dashoffset: 0; }
    }
    .flow-line   { stroke-dasharray: 6 5; animation: flowDash 1.8s linear infinite; }
    .flow-line-b { animation-delay: 0.45s; }
    .flow-line-c { animation-delay: 0.9s;  }
    .flow-line-d { animation-delay: 1.35s; }

    /* ── Glitch effect on ASCII name ── */
    @keyframes glitch1 {
      0%,88%,100% { clip-path: none; transform: translate(0); }
      89%          { clip-path: inset(8% 0 72% 0);  transform: translate(-2px, 0); }
      91%          { clip-path: inset(58% 0 8% 0);  transform: translate(2px, 0);  }
      93%          { clip-path: none; transform: translate(0); }
    }
    @keyframes glitch2 {
      0%,90%,100% { clip-path: none; transform: translate(0); opacity: 0; }
      91%          { clip-path: inset(20% 0 48% 0); transform: translate(3px, 0); opacity: 0.55; }
      93%          { clip-path: inset(48% 0 20% 0); transform: translate(-3px,0); opacity: 0.55; }
      95%          { opacity: 0; }
    }
    .name-main  { animation: glitch1 7s steps(1) infinite; }
    .name-ghost { animation: glitch2 7s steps(1) infinite; fill: #9b59b6; }

    /* ── Grid shimmer ── */
    @keyframes gridShimmer {
      0%,100% { opacity: 0.35; }
      50%     { opacity: 0.85; }
    }
    .bg-grid { animation: gridShimmer 9s ease-in-out infinite; }

    /* ── Corner blink ── */
    @keyframes bracketBlink {
      0%,100% { opacity: 1; }
      50%     { opacity: 0.2; }
    }
    .corner-br { animation: bracketBlink 2.2s ease-in-out infinite; }

    /* ── Scan line ── */
    @keyframes scanLine {
      0%   { transform: translateY(-450px); opacity: 0.035; }
      100% { transform: translateY(450px);  opacity: 0.035; }
    }
    .scan-line { animation: scanLine 5.5s linear infinite; }

    /* ── Status dot blink ── */
    @keyframes statusBlink {
      0%,100% { opacity: 1; }
      50%     { opacity: 0.1; }
    }
    .status-dot { animation: statusBlink 1.2s ease-in-out infinite; }

    /* ── Divider glow pulse ── */
    @keyframes dividerPulse {
      0%,100% { opacity: 0.3; }
      50%     { opacity: 0.7; }
    }
    .divider-line { animation: dividerPulse 3s ease-in-out infinite; }
  </style>

  <!-- ═══════════════════ BACKGROUND ═══════════════════ -->
  <g clip-path="url(#svgClip)">
    <rect width="900" height="450" fill="url(#bgGlow)"/>
    <rect class="bg-grid" width="900" height="450" fill="url(#grid)"/>
    <rect width="900" height="450" fill="url(#hexGrid)" opacity="0.6"/>

    <!-- Scan line sweep -->
    <rect class="scan-line" x="0" y="0" width="900" height="3" fill="#00f0ff"/>

    <!-- Ambient glows -->
    <ellipse cx="250" cy="190" rx="240" ry="160"
             fill="#00d4ff" opacity="0.025"/>
    <ellipse cx="718" cy="225" rx="190" ry="160"
             fill="#8a2be2" opacity="0.03"/>

    <!-- Top / bottom accent lines -->
    <line x1="0" y1="1"   x2="900" y2="1"   stroke="#00f0ff" stroke-width="1.5" opacity="0.55"/>
    <line x1="0" y1="449" x2="900" y2="449" stroke="#8a2be2" stroke-width="1.5" opacity="0.55"/>

    <!-- Vertical divider between panels -->
    <line class="divider-line" x1="520" y1="16" x2="520" y2="428"
          stroke="#00f0ff" stroke-width="0.8"/>

    <!-- Corner brackets TL -->
    <g class="corner-br" opacity="0.75">
      <line x1="14" y1="14" x2="42" y2="14" stroke="#00f0ff" stroke-width="1.8"/>
      <line x1="14" y1="14" x2="14" y2="42" stroke="#00f0ff" stroke-width="1.8"/>
    </g>
    <!-- Corner brackets TR -->
    <g class="corner-br" opacity="0.75">
      <line x1="886" y1="14" x2="858" y2="14" stroke="#00f0ff" stroke-width="1.8"/>
      <line x1="886" y1="14" x2="886" y2="42" stroke="#00f0ff" stroke-width="1.8"/>
    </g>
    <!-- Corner brackets BL -->
    <g class="corner-br" opacity="0.75">
      <line x1="14" y1="436" x2="42" y2="436" stroke="#8a2be2" stroke-width="1.8"/>
      <line x1="14" y1="436" x2="14" y2="408" stroke="#8a2be2" stroke-width="1.8"/>
    </g>
    <!-- Corner brackets BR -->
    <g class="corner-br" opacity="0.75">
      <line x1="886" y1="436" x2="858" y2="436" stroke="#8a2be2" stroke-width="1.8"/>
      <line x1="886" y1="436" x2="886" y2="408" stroke="#8a2be2" stroke-width="1.8"/>
    </g>

    <!-- ═══════════════════ LEFT PANEL — PROFILE CARD ═══════════════════ -->

    <!-- Glass card -->
    <rect x="16" y="16" width="490" height="418" rx="10"
          fill="url(#panelGrad)"
          stroke="#1e3a5f" stroke-width="1" opacity="0.9"/>
    <!-- Card top highlight -->
    <rect x="17" y="17" width="488" height="1" rx="1"
          fill="#58a6ff" opacity="0.25"/>

    <!-- Card header strip -->
    <rect x="16" y="16" width="490" height="36" rx="10"
          fill="#0d1b2e" fill-opacity="0.9"/>
    <line x1="16" y1="52" x2="506" y2="52" stroke="#1e3a5f" stroke-width="1" opacity="1"/>

    <!-- Header label -->
    <text x="36" y="38" class="font-inter"
          font-size="9" font-weight="700" letter-spacing="3.5"
          fill="#58a6ff" opacity="0.95">NITHISH-O7  ·  COMMAND INTERFACE  ·  v5.0</text>

    <!-- Status dot -->
    <circle class="status-dot" cx="488" cy="34" r="4.5" fill="#39d353"/>

    <!-- ── ASCII Art Name ── -->
    <!-- Ghost layer for glitch effect -->
    <text class="name-ghost font-mono" x="16" y="72"
          font-size="8.6" font-weight="700" letter-spacing="0.5"
          xml:space="preserve">
      <tspan x="16" dy="0">███╗   ██╗██╗████████╗██╗  ██╗██╗███████╗██╗  ██╗       ██████╗ ███████╗</tspan>
      <tspan x="16" dy="16">████╗  ██║██║╚══██╔══╝██║  ██║██║██╔════╝██║  ██║      ██╔═══██╗╚════██║</tspan>
      <tspan x="16" dy="16">██╔██╗ ██║██║   ██║   ███████║██║███████╗███████║█████╗██║   ██║    ██╔╝</tspan>
      <tspan x="16" dy="16">██║╚██╗██║██║   ██║   ██╔══██║██║╚════██║██╔══██║╚════╝██║   ██║   ██╔╝ </tspan>
      <tspan x="16" dy="16">██║ ╚████║██║   ██║   ██║  ██║██║███████║██║  ██║      ╚██████╔╝   ██║  </tspan>
      <tspan x="16" dy="16">╚═╝  ╚═══╝╚═╝   ╚═╝   ╚═╝  ╚═╝╚═╝╚══════╝╚═╝  ╚═╝       ╚═════╝    ╚═╝  </tspan>
    </text>

    <!-- Main ASCII art layer with very subtle cyan glow -->
    <text class="name-main font-mono" x="16" y="72"
          filter="url(#nameGlow)"
          font-size="8.6" font-weight="700" fill="#00f0ff" letter-spacing="0.5"
          xml:space="preserve">
      <tspan x="16" dy="0">███╗   ██╗██╗████████╗██╗  ██╗██╗███████╗██╗  ██╗       ██████╗ ███████╗</tspan>
      <tspan x="16" dy="16">████╗  ██║██║╚══██╔══╝██║  ██║██║██╔════╝██║  ██║      ██╔═══██╗╚════██║</tspan>
      <tspan x="16" dy="16">██╔██╗ ██║██║   ██║   ███████║██║███████╗███████║█████╗██║   ██║    ██╔╝</tspan>
      <tspan x="16" dy="16">██║╚██╗██║██║   ██║   ██╔══██║██║╚════██║██╔══██║╚════╝██║   ██║   ██╔╝ </tspan>
      <tspan x="16" dy="16">██║ ╚████║██║   ██║   ██║  ██║██║███████║██║  ██║      ╚██████╔╝   ██║  </tspan>
      <tspan x="16" dy="16">╚═╝  ╚═══╝╚═╝   ╚═╝   ╚═╝  ╚═╝╚═╝╚══════╝╚═╝  ╚═╝       ╚═════╝    ╚═╝  </tspan>
    </text>

    <!-- ── Separator line under name ── -->
    <line x1="26" y1="182" x2="500" y2="182"
          stroke="#00f0ff" stroke-width="0.6" opacity="0.2"/>

    <!-- ── TERMINAL READOUT ── -->
    <!-- Terminal shell background -->
    <rect x="26" y="192" width="474" height="122" rx="6"
          fill="#060e1a" fill-opacity="0.85"
          stroke="#1a3352" stroke-width="0.8"/>
    <!-- Terminal top bar -->
    <rect x="26" y="192" width="474" height="22" rx="6"
          fill="#0d1b2e" fill-opacity="1"/>
    <!-- Terminal dots -->
    <circle cx="42" cy="203" r="5" fill="#ff5f57"/>
    <circle cx="56" cy="203" r="5" fill="#febc2e"/>
    <circle cx="70" cy="203" r="5" fill="#28c840"/>
    <!-- Terminal title — large and prominent -->
    <text x="263" y="207" class="font-mono" font-size="13" font-weight="600" text-anchor="middle"
          fill="#5a8ab0" letter-spacing="1.5">nithish@o7  ~  profile.sh</text>
    <!-- Terminal lines -->
    <text x="36" y="233" class="font-mono" font-size="11" fill="#39d353">$</text>
    <text x="49" y="233" class="font-mono" font-size="11" fill="#8b949e"> cat</text>
    <text x="88" y="233" class="font-mono" font-size="11" fill="#00f0ff"> /dev/nithish</text>
    <text x="36" y="252" class="font-mono" font-size="10.5" fill="#5a7a8a">  ROLE    </text>
    <text x="96" y="252" class="font-mono" font-size="10.5" fill="#4a6a8a">→</text>
    <text x="110" y="252" class="font-mono" font-size="10.5" fill="#e0aaff">  ServiceNow Developer</text>
    <text x="36" y="270" class="font-mono" font-size="10.5" fill="#5a7a8a">  BASE    </text>
    <text x="96" y="270" class="font-mono" font-size="10.5" fill="#4a6a8a">→</text>
    <text x="110" y="270" class="font-mono" font-size="10.5" fill="#ffb74d">  Tamil Nadu, India / IST+5:30</text>
    <text x="36" y="288" class="font-mono" font-size="10.5" fill="#5a7a8a">  STATUS  </text>
    <text x="96" y="288" class="font-mono" font-size="10.5" fill="#4a6a8a">→</text>
    <text x="110" y="288" class="font-mono" font-size="10.5" fill="#67e8f9">  Building · Automating · Shipping</text>

    <!-- ── Separator ── -->
    <line x1="26" y1="330" x2="500" y2="330"
          stroke="#1e3a5f" stroke-width="0.8" opacity="0.9"/>

    <!-- ── Role Badge Tags ── -->
    <!-- Tag 1: ServiceNow -->
    <g class="badge-1">
      <rect x="26" y="345" width="130" height="32" rx="6"
            fill="#8a2be2" fill-opacity="0.25"
            stroke="#8a2be2" stroke-width="1" stroke-opacity="0.8"/>
      <text x="91" y="366" class="font-inter"
            font-size="11" font-weight="700" text-anchor="middle" letter-spacing="1.5"
            fill="#e0aaff">SERVICENOW</text>
    </g>

    <!-- Tag 2: App Developer -->
    <g class="badge-2">
      <rect x="168" y="345" width="155" height="32" rx="6"
            fill="#ff9500" fill-opacity="0.15"
            stroke="#ff9500" stroke-width="1" stroke-opacity="0.75"/>
      <text x="245" y="366" class="font-inter"
            font-size="11" font-weight="700" text-anchor="middle" letter-spacing="1"
            fill="#ffb74d">APP DEVELOPER</text>
    </g>

    <!-- Tag 3: Workflow Designer -->
    <g class="badge-3">
      <rect x="335" y="345" width="165" height="32" rx="6"
            fill="#00d4ff" fill-opacity="0.12"
            stroke="#00d4ff" stroke-width="1" stroke-opacity="0.75"/>
      <text x="417" y="366" class="font-inter"
            font-size="11" font-weight="700" text-anchor="middle" letter-spacing="1"
            fill="#67e8f9">WORKFLOW DESIGN</text>
    </g>

    <!-- ═══════════════════ RIGHT PANEL — DEV ECOSYSTEM ═══════════════════ -->

    <!-- Right glass card -->
    <rect x="530" y="16" width="354" height="418" rx="10"
          fill="url(#panelGradR)"
          stroke="#2d1b4e" stroke-width="1" opacity="0.9"/>
    <rect x="531" y="17" width="352" height="1" rx="1"
          fill="#8a2be2" opacity="0.3"/>

    <!-- Right panel header -->
    <rect x="530" y="16" width="354" height="36" rx="10"
          fill="#0d0a1e" fill-opacity="0.9"/>
    <line x1="530" y1="52" x2="884" y2="52" stroke="#2d1b4e" stroke-width="1" opacity="1"/>

    <text x="548" y="38" class="font-inter"
          font-size="9" font-weight="700" letter-spacing="2.5"
          fill="#9b72cf" opacity="0.9">DEV ECOSYSTEM  ·  NODE MAP</text>

    <!-- Data flow lines (static) -->
    <line x1="718" y1="205" x2="620" y2="125" stroke="#00f0ff" stroke-width="1" opacity="0.2"/>
    <line x1="718" y1="205" x2="816" y2="125" stroke="#8a2be2" stroke-width="1" opacity="0.2"/>
    <line x1="718" y1="205" x2="816" y2="285" stroke="#00f0ff" stroke-width="1" opacity="0.2"/>
    <line x1="718" y1="205" x2="620" y2="285" stroke="#8a2be2" stroke-width="1" opacity="0.2"/>

    <!-- Animated flow lines -->
    <line class="flow-line"              x1="718" y1="205" x2="620" y2="125" stroke="#00f0ff" stroke-width="2" opacity="0.75"/>
    <line class="flow-line flow-line-b"  x1="718" y1="205" x2="816" y2="125" stroke="#8a2be2" stroke-width="2" opacity="0.75"/>
    <line class="flow-line flow-line-c"  x1="718" y1="205" x2="816" y2="285" stroke="#00f0ff" stroke-width="2" opacity="0.75"/>
    <line class="flow-line flow-line-d"  x1="718" y1="205" x2="620" y2="285" stroke="#8a2be2" stroke-width="2" opacity="0.75"/>

    <!-- ── CENTRAL NODE ── -->
    <circle class="ring-pulse"   cx="718" cy="205" r="30"
            fill="none" stroke="#00f0ff" stroke-width="1.5" opacity="0.65"/>
    <circle class="ring-pulse-2" cx="718" cy="205" r="30"
            fill="none" stroke="#8a2be2" stroke-width="1"   opacity="0.4"/>

    <!-- Radar sweep -->
    <g class="radar-sweep">
      <path d="M718,205 L718,163 A44,44 0 0,1 749,173 Z"
            fill="#00f0ff" fill-opacity="0.06"/>
      <line x1="718" y1="205" x2="718" y2="160"
            stroke="#00f0ff" stroke-width="1.2" opacity="0.5"/>
    </g>

    <!-- Central node body -->
    <g class="node-pulse" filter="url(#nodeGlow)">
      <circle cx="718" cy="205" r="24"
              fill="#00f0ff" fill-opacity="0.07"
              stroke="#00f0ff" stroke-width="1.8" opacity="0.95"/>
      <circle cx="718" cy="205" r="15"
              fill="#070d1a" stroke="#00f0ff" stroke-width="2.2"/>
      <line x1="708" y1="205" x2="728" y2="205" stroke="#00f0ff" stroke-width="1.2" opacity="0.9"/>
      <line x1="718" y1="195" x2="718" y2="215" stroke="#00f0ff" stroke-width="1.2" opacity="0.9"/>
      <circle cx="718" cy="205" r="3.5" fill="#00f0ff"/>
    </g>

    <!-- Central CORE label -->
    <text x="718" y="238" class="font-inter"
          font-size="9" font-weight="700" text-anchor="middle" letter-spacing="2"
          fill="#00f0ff" opacity="0.65">CORE</text>

    <!-- ── SATELLITE NODE A — top-left (SNOW) ── -->
    <g class="sat-a" filter="url(#purpleGlow)">
      <circle cx="620" cy="125" r="13"
              fill="#070d1a" stroke="#00f0ff" stroke-width="1.8"/>
      <circle cx="620" cy="125" r="5.5" fill="#00f0ff" opacity="0.85"/>
    </g>
    <text x="620" y="103" class="font-inter"
          font-size="8.5" font-weight="700" text-anchor="middle" letter-spacing="1.2"
          fill="#00f0ff" opacity="0.9">SNOW</text>

    <!-- ── SATELLITE NODE B — top-right (JAVA / JS) ── -->
    <g class="sat-b" filter="url(#purpleGlow)">
      <circle cx="816" cy="125" r="13"
              fill="#070d1a" stroke="#8a2be2" stroke-width="1.8"/>
      <circle cx="816" cy="125" r="5.5" fill="#8a2be2" opacity="0.85"/>
    </g>
    <text x="816" y="103" class="font-inter"
          font-size="8.5" font-weight="700" text-anchor="middle" letter-spacing="1"
          fill="#8a2be2" opacity="0.9">JAVA/JS</text>

    <!-- ── SATELLITE NODE C — bottom-right (AUTOMATION) ── -->
    <g class="sat-c" filter="url(#purpleGlow)">
      <circle cx="816" cy="285" r="13"
              fill="#070d1a" stroke="#00f0ff" stroke-width="1.8"/>
      <circle cx="816" cy="285" r="5.5" fill="#00f0ff" opacity="0.85"/>
    </g>
    <text x="816" y="313" class="font-inter"
          font-size="8.5" font-weight="700" text-anchor="middle" letter-spacing="1"
          fill="#00f0ff" opacity="0.9">AUTO</text>

    <!-- ── SATELLITE NODE D — bottom-left (API) ── -->
    <g class="sat-d" filter="url(#purpleGlow)">
      <circle cx="620" cy="285" r="13"
              fill="#070d1a" stroke="#8a2be2" stroke-width="1.8"/>
      <circle cx="620" cy="285" r="5.5" fill="#8a2be2" opacity="0.85"/>
    </g>
    <text x="620" y="313" class="font-inter"
          font-size="8.5" font-weight="700" text-anchor="middle" letter-spacing="1"
          fill="#8a2be2" opacity="0.9">API</text>

  </g>
</svg>
